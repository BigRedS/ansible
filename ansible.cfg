[defaults]
inventory = inventory/hosts.yaml
roles_path = roles
host_key_checking = True
retry_files_enabled = False

[privilege_escalation]
# Deliberately NOT setting become_ask_pass = True here: any privilege escalation
# has to be manually requested and authorised by a human; agents certainly shouldn't
# be able to just become root everywhere
